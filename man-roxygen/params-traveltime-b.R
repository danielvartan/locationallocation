#' @param traveltime A [`list`][base::list] with the output of the
#'   [`traveltime()`][traveltime()] function. If not provided, the function will
#'   run the [`traveltime()`][traveltime()] function based on the provided
#'   parameters.
